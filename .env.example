# Database Configuration
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/projectsdb

# Security
API_KEY=dev-secret-key

# Pagination
PAGINATION_MAX_PAGE_SIZE=100

# Application Settings
DEBUG=False
APP_NAME=FastAPI Project Tasks
APP_VERSION=1.0.0

# Docker Configuration (when running with docker-compose)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/projectsdb
